import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { ArrowLeft, Clock3, CreditCard, RotateCcw, Smartphone, type LucideIcon } from "lucide-react";
import { useEffect, useState } from "react";
import { AppShell } from "@/components/app-shell";
import { Button } from "@/components/ui/button";

export const Route = createFileRoute("/hold")({ head: () => ({ meta: [{ title: "Complete your booking — VERA HOLIDAYS" }, { name: "description", content: "Secure payment for your held reservation." }, { property: "og:title", content: "Complete your booking" }, { property: "og:description", content: "Your VERA HOLIDAYS reservation is temporarily held." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: HoldPage });

function HoldPage(){const methods:["card"|"upi",LucideIcon,string][]=[["card",CreditCard,"Card"],["upi",Smartphone,"UPI"]];const [seconds,setSeconds]=useState(300);const [method,setMethod]=useState<"card"|"upi">("card");const navigate=useNavigate();useEffect(()=>{if(seconds<=0)return;const id=setInterval(()=>setSeconds(s=>Math.max(0,s-1)),1000);return()=>clearInterval(id)},[seconds]);const time=`${String(Math.floor(seconds/60)).padStart(2,"0")}:${String(seconds%60).padStart(2,"0")}`;return <AppShell><main className="mx-auto max-w-5xl px-5 py-10"><Link to="/hotel" className="inline-flex items-center gap-2 text-sm text-muted-foreground"><ArrowLeft className="size-4"/>Back to room</Link>{seconds===0?<div className="mx-auto mt-24 max-w-xl text-center"><div className="mx-auto grid size-16 place-items-center rounded-full bg-brand-soft text-primary"><RotateCcw/></div><h1 className="mt-6 text-3xl font-bold">Your hold has expired</h1><p className="mt-3 text-muted-foreground">But the room’s probably still here — search again and we’ll check it for you.</p><Button className="mt-7" size="lg" asChild><Link to="/search">Search again</Link></Button></div>:<><div className="mt-7 rounded-2xl bg-brand-soft px-6 py-5 text-center"><p className="flex items-center justify-center gap-2 text-sm font-semibold text-primary"><Clock3 className="size-4"/>THIS ROOM IS HELD FOR YOU</p><p className={`mt-2 text-5xl font-bold tabular-nums text-primary ${seconds<60?"animate-hold-pulse":""}`}>{time}</p><p className="mt-2 text-sm text-muted-foreground">Complete payment before the timer ends.</p></div><div className="mt-8 grid gap-8 lg:grid-cols-[1fr_330px]"><section><h1 className="text-2xl font-bold">How would you like to pay?</h1><div className="mt-5 grid grid-cols-2 gap-3">{methods.map(([value,Icon,label])=><Button key={value} variant={method===value?"default":"outline"} className="h-14 rounded-xl" onClick={()=>setMethod(value)}><Icon/>{label}</Button>)}</div>{method==="card"?<div className="mt-6 grid gap-4"><label className="text-sm font-medium">Card number<input className="mt-2 h-12 w-full rounded-xl border border-input px-4 outline-none focus:ring-2 focus:ring-ring" placeholder="4242 4242 4242 4242"/></label><label className="text-sm font-medium">Name on card<input className="mt-2 h-12 w-full rounded-xl border border-input px-4 outline-none focus:ring-2 focus:ring-ring" placeholder="Your full name"/></label><div className="grid grid-cols-2 gap-4"><label className="text-sm font-medium">Expiry<input className="mt-2 h-12 w-full rounded-xl border border-input px-4" placeholder="MM / YY"/></label><label className="text-sm font-medium">CVV<input className="mt-2 h-12 w-full rounded-xl border border-input px-4" placeholder="•••"/></label></div></div>:<label className="mt-6 block text-sm font-medium">UPI ID<input className="mt-2 h-12 w-full rounded-xl border border-input px-4" placeholder="name@bank"/></label>}</section><aside className="h-fit rounded-2xl border border-border p-6"><h2 className="font-semibold">The Rose Courtyard Haveli</h2><p className="mt-1 text-sm text-muted-foreground">12–15 Oct · 2 guests</p><div className="my-5 border-t border-border"/><p className="flex justify-between font-semibold"><span>Total</span><span className="text-primary">₹14,562</span></p><Button size="lg" className="mt-6 w-full" onClick={()=>navigate({to:"/confirmation"})}>Pay now</Button><p className="mt-3 text-center text-xs text-muted-foreground">Secure payment · No hidden fees</p></aside></div></>}</main></AppShell>}