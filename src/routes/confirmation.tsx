import { createFileRoute, Link } from "@tanstack/react-router";
import { CalendarDays, Check, MapPin } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { Button } from "@/components/ui/button";

export const Route = createFileRoute("/confirmation")({ head: () => ({ meta: [{ title: "Booking confirmed — VERA HOLIDAYS" }, { name: "description", content: "Your VERA HOLIDAYS booking is confirmed." }, { property: "og:title", content: "Booking confirmed" }, { property: "og:description", content: "Your VERA HOLIDAYS itinerary is ready." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: Confirmation });
function Confirmation(){return <AppShell><main className="mx-auto max-w-2xl px-5 py-20 text-center"><div className="mx-auto grid size-20 place-items-center rounded-full bg-success text-primary-foreground animate-check-pop"><Check className="size-10" strokeWidth={3}/></div><p className="mt-7 text-sm font-semibold text-success">BOOKING CONFIRMED</p><h1 className="mt-2 text-4xl font-bold">Jaipur is waiting</h1><p className="mt-3 text-muted-foreground">We’ve sent the details to your email. Everything is set.</p><div className="mt-9 rounded-2xl border border-border p-6 text-left shadow-soft"><div className="flex justify-between gap-4 border-b border-border pb-5"><div><p className="text-xs text-muted-foreground">BOOKING REFERENCE</p><p className="mt-1 text-xl font-bold">VH8K2P</p></div><span className="h-fit rounded-full bg-success/15 px-3 py-1 text-xs font-semibold text-success">Confirmed</span></div><h2 className="mt-5 text-lg font-semibold">The Rose Courtyard Haveli</h2><p className="mt-2 flex items-center gap-2 text-sm text-muted-foreground"><MapPin className="size-4"/>Bani Park, Jaipur</p><p className="mt-3 flex items-center gap-2 text-sm text-muted-foreground"><CalendarDays className="size-4"/>12–15 October 2026 · 3 nights</p><div className="mt-5 flex justify-between border-t border-border pt-5"><span>Total paid</span><strong>₹14,562</strong></div></div><Button className="mt-7" size="lg" asChild><Link to="/bookings">View my bookings</Link></Button></main></AppShell>}